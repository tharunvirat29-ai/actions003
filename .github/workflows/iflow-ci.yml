name: iFlow Test

on:
  push:
    branches: [main]

jobs:
  test-iflow:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      
      # This is our new step
      - name: Check for hardcoded URLs
        run: '! grep -r "https://webhook.site/6c7154ae-36c5-4cef-9b2f-bf8a86b27a3a"'
